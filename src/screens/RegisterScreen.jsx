
import { Text, View } from "react-native";

const RegisterScreen = ({ navigation }) => {
  return (
    <View>
        <Text>Ini Adalah RegisterScreen</Text>
    </View>
  )
};

export default RegisterScreen;
